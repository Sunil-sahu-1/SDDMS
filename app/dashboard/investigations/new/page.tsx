"use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; import PageHeader from "@/components/PageHeader"; import {investigationsApi} from "@/services/api";
export default function NewStatement(){const router=useRouter();const [busy,setBusy]=useState(false);const [form,setForm]=useState({statement_number:"",case_number:"",witness_name:"",statement:""});const set=(k:string,v:string)=>setForm({...form,[k]:v});const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);try{await investigationsApi.create(form);router.push("/dashboard/investigations")}catch{alert("Witness statement API is not reachable. The form is ready for backend integration.")}finally{setBusy(false)}};return <div className="space-y-5"><PageHeader eyebrow="Investigation intake" title="New witness statement" description="Create a protected statement record. Sensitive witness data should be encrypted and permissioned by the backend."/><form onSubmit={submit} className="card p-5 sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><div><label className="label">Statement number</label><input className="input" value={form.statement_number} onChange={e=>set("statement_number",e.target.value)} placeholder="WS/041/03"/></div><div><label className="label">Case number</label><input className="input" value={form.case_number} onChange={e=>set("case_number",e.target.value)} required placeholder="CASE-2026-041"/></div><div><label className="label">Witness reference</label><input className="input" value={form.witness_name} onChange={e=>set("witness_name",e.target.value)} placeholder="Use protected identifier where required"/></div><div><label className="label">Classification</label><select className="input"><option>Confidential</option><option>Highly Confidential</option><option>Restricted</option></select></div><div className="sm:col-span-2"><label className="label">Statement</label><textarea className="input min-h-60" value={form.statement} onChange={e=>set("statement",e.target.value)} required/></div></div><div className="mt-6 flex justify-end gap-2 border-t border-slate-100 pt-5"><button type="button" onClick={()=>router.back()} className="btn-secondary">Cancel</button><button disabled={busy} className="btn-primary">{busy?"Saving…":"Save statement"}</button></div></form></div>}
