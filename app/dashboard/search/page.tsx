"use client"; import {useState} from "react"; import {Search,FileText,FolderOpen,Database,BrainCircuit} from "lucide-react"; import PageHeader from "@/components/PageHeader"; import {searchApi} from "@/services/api";
const demo=[{kind:"Document",title:"Forensic Report — FR/2026/019",meta:"CASE-2026-041 • Verified",icon:FileText},{kind:"Case",title:"CASE-2026-041 — Digital Evidence",meta:"Under Investigation • High priority",icon:FolderOpen},{kind:"Evidence",title:"EV-2026-0142 — Mobile device",meta:"CASE-2026-041 • Verified",icon:Database}];
export default function SearchPage(){const [q,setQ]=useState("");const [results,setResults]=useState<any[]>(demo);const [loading,setLoading]=useState(false);const run=async()=>{if(!q)return;setLoading(true);try{const r:any=await searchApi.global(q);setResults(Array.isArray(r)?r:r?.results??[])}catch{setResults(demo.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())))}finally{setLoading(false)}};return <div className="space-y-5"><PageHeader eyebrow="Discovery" title="Advanced search" description="Search authorized cases, documents, evidence and investigation records from one workspace."/><div className="card p-4"><div className="flex flex-col gap-2 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={18}/><input onKeyDown={e=>e.key==="Enter"&&run()} value={q} onChange={e=>setQ(e.target.value)} className="input pl-10" placeholder="Try: CASE-2026-041 forensic report"/></div><button onClick={run} className="btn-primary"><Search size={16}/>{loading?"Searching…":"Search"}</button></div><div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-500"><span className="rounded-full bg-slate-100 px-3 py-1">All modules</span><span className="rounded-full bg-slate-100 px-3 py-1">Verified only</span><span className="rounded-full bg-slate-100 px-3 py-1">Recent 30 days</span><span className="rounded-full bg-violet-50 px-3 py-1 text-violet-700"><BrainCircuit size={13} className="mr-1 inline"/>AI-assisted search available</span></div></div><section className="card divide-y divide-slate-100">{results.map((r:any,i)=>{const Icon=r.icon??FileText;return <div key={i} className="flex items-start gap-4 p-5 transition hover:bg-slate-50"><div className="rounded-xl bg-blue-50 p-2.5 text-blue-600"><Icon size={18}/></div><div><p className="text-xs font-bold uppercase tracking-wide text-slate-400">{r.kind??r.type??"Record"}</p><h3 className="mt-1 font-bold text-slate-800">{r.title??r.name??r.document_title??"Search result"}</h3><p className="mt-1 text-xs text-slate-500">{r.meta??r.description??r.case_number??"Authorized record"}</p></div></div>})}</section></div>}
