"use client";
import { useState } from "react";
import Link from "next/link";
import { ArrowRight, FileCheck2, ShieldCheck, Upload, UserRound, LockKeyhole, AlertCircle } from "lucide-react";
import { authApi } from "@/services/api";

const roles = [
  ["NORMAL_USER", "Normal User", "Complaint submission and complaint status only."],
  ["POLICE_OFFICER", "Police Officer", "Requires departmental identity verification."],
  ["INVESTIGATOR", "Investigator", "Requires investigator/service verification."],
  ["LEGAL_OFFICER", "Legal Officer", "Requires legal department verification."],
] as const;

export default function RegisterPage() {
  const [role, setRole] = useState("NORMAL_USER");
  const [form, setForm] = useState({username:"",email:"",password:"",confirmPassword:"",first_name:"",last_name:"",phone:"",department:"",designation:"",proof_number:""});
  const [proof,setProof]=useState<File|null>(null); const [photo,setPhoto]=useState<File|null>(null);
  const [busy,setBusy]=useState(false); const [error,setError]=useState(""); const [success,setSuccess]=useState(false);
  const staff=role!=="NORMAL_USER"; const update=(key:string,value:string)=>setForm(v=>({...v,[key]:value}));
  const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");if(form.password!==form.confirmPassword){setError("Passwords do not match.");return}if(!proof||!photo){setError("Proof document and profile photo are required for validation.");return}setBusy(true);try{const data=new FormData();Object.entries(form).forEach(([k,v])=>{if(k!=="confirmPassword")data.append(k,v)});data.append("role",role);data.append("verification_type",role);data.append("proof_document",proof);data.append("photo",photo);await authApi.registerApplication(data);setSuccess(true)}catch(err:any){const d=err?.data as any;const flat=d&&typeof d==="object"?Object.values(d).flatMap((x:any)=>Array.isArray(x)?x:[String(x)]).join(" "):"";setError(d?.message||d?.detail||flat||err?.message||"Registration failed.")}finally{setBusy(false)}};
  if(success)return <main className="min-h-screen bg-[#f5f7fb] flex items-center justify-center p-5"><div className="card w-full max-w-xl p-7 text-center sm:p-10"><div className="mx-auto mb-5 flex h-16 w-16 items-center justify-center rounded-2xl bg-emerald-50 text-emerald-600"><FileCheck2 size={30}/></div><p className="text-sm font-semibold text-emerald-600">Application submitted</p><h1 className="mt-2 text-3xl font-bold text-slate-900">Your account is awaiting validation</h1><p className="mt-4 text-sm leading-6 text-slate-500">An administrator will review your identity and supporting documents. You will be able to sign in after approval.</p><div className="mt-7 rounded-2xl border border-blue-100 bg-blue-50 p-4 text-left text-sm text-blue-800"><strong>Selected account:</strong> {roles.find(r=>r[0]===role)?.[1]}<br/><span className="text-blue-700">Do not submit another application while this one is under review.</span></div><Link href="/login" className="btn-primary mt-7 w-full">Return to sign in <ArrowRight size={17}/></Link></div></main>;
  return <main className="min-h-screen bg-[#f5f7fb] lg:grid lg:grid-cols-[.9fr_1.1fr]"><section className="hidden overflow-hidden bg-navy p-12 text-white lg:flex lg:flex-col lg:justify-between"><div><div className="flex items-center gap-3"><div className="rounded-xl bg-white/10 p-2.5"><ShieldCheck size={26}/></div><div><p className="font-bold">Secure DMS</p><p className="text-xs text-blue-100">Law & Investigation Records</p></div></div><div className="mt-24 max-w-xl"><p className="text-sm font-semibold uppercase tracking-[.2em] text-blue-200">Controlled registration</p><h1 className="mt-4 text-5xl font-bold leading-tight">Apply for secure access.</h1><p className="mt-6 text-lg leading-8 text-blue-100">Every account is validated before protected records become available. Normal users remain limited to their own complaint workflow.</p></div></div><div className="grid grid-cols-2 gap-3 text-sm"><div className="glass rounded-xl p-4"><FileCheck2 size={18}/><p className="mt-2 font-semibold">Identity validation</p></div><div className="glass rounded-xl p-4"><LockKeyhole size={18}/><p className="mt-2 font-semibold">Role-based access</p></div></div></section><section className="flex justify-center p-5 sm:p-8 lg:p-12"><div className="w-full max-w-3xl"><div className="mb-7"><div className="mb-5 inline-flex items-center gap-3 lg:hidden"><div className="rounded-xl bg-navy p-2.5 text-white"><ShieldCheck size={24}/></div><span className="font-bold text-navy">Secure DMS</span></div><p className="text-sm font-semibold text-blue-600">Account application</p><h2 className="mt-1 text-3xl font-bold text-slate-900">Create your access request</h2><p className="mt-2 text-sm text-slate-500">Choose the account category that matches your role. Administrator accounts are created separately.</p></div><form onSubmit={submit} className="card space-y-7 p-5 sm:p-8"><div><label className="label">Account category</label><div className="grid gap-3 sm:grid-cols-2">{roles.map(r=><button type="button" key={r[0]} onClick={()=>setRole(r[0])} className={`rounded-2xl border p-4 text-left transition ${role===r[0]?"border-blue-500 bg-blue-50 shadow-sm":"border-slate-200 bg-white hover:border-slate-300"}`}><div className="flex items-start gap-3"><UserRound size={18} className={role===r[0]?"text-blue-600":"text-slate-400"}/><div><p className="font-semibold text-slate-900">{r[1]}</p><p className="mt-1 text-xs leading-5 text-slate-500">{r[2]}</p></div></div></button>)}</div></div><div><p className="mb-4 font-semibold text-slate-900">Personal details</p><div className="grid gap-4 sm:grid-cols-2"><div><label className="label">First name</label><input className="input" value={form.first_name} onChange={e=>update("first_name",e.target.value)} required/></div><div><label className="label">Last name</label><input className="input" value={form.last_name} onChange={e=>update("last_name",e.target.value)} required/></div><div><label className="label">Username</label><input className="input" value={form.username} onChange={e=>update("username",e.target.value)} required/></div><div><label className="label">Email</label><input type="email" className="input" value={form.email} onChange={e=>update("email",e.target.value)} required/></div><div><label className="label">Phone</label><input className="input" value={form.phone} onChange={e=>update("phone",e.target.value)} required/></div></div></div><div><p className="mb-4 font-semibold text-slate-900">Verification details</p><div className="grid gap-4 sm:grid-cols-2"><div><label className="label">Proof / service ID number</label><input className="input" value={form.proof_number} onChange={e=>update("proof_number",e.target.value)} required/></div><div><label className="label">Department</label><input className="input" value={form.department} onChange={e=>update("department",e.target.value)} required={staff}/></div>{staff&&<div><label className="label">Designation</label><input className="input" value={form.designation} onChange={e=>update("designation",e.target.value)} required/></div>}<div><label className="label">Proof document</label><label className="flex cursor-pointer items-center gap-3 rounded-xl border border-dashed border-slate-300 bg-slate-50 p-3 text-sm text-slate-600"><Upload size={17}/><span className="truncate">{proof?.name||"Choose document"}</span><input className="hidden" type="file" onChange={e=>setProof(e.target.files?.[0]||null)} required/></label></div><div><label className="label">Profile photo</label><label className="flex cursor-pointer items-center gap-3 rounded-xl border border-dashed border-slate-300 bg-slate-50 p-3 text-sm text-slate-600"><Upload size={17}/><span className="truncate">{photo?.name||"Choose photo"}</span><input className="hidden" type="file" accept="image/*" onChange={e=>setPhoto(e.target.files?.[0]||null)} required/></label></div></div></div><div><p className="mb-4 font-semibold text-slate-900">Secure password</p><div className="grid gap-4 sm:grid-cols-2"><div><label className="label">Password</label><input type="password" className="input" minLength={8} value={form.password} onChange={e=>update("password",e.target.value)} required/></div><div><label className="label">Confirm password</label><input type="password" className="input" minLength={8} value={form.confirmPassword} onChange={e=>update("confirmPassword",e.target.value)} required/></div></div></div>{error&&<div className="flex gap-2 rounded-xl border border-red-100 bg-red-50 p-3 text-sm text-red-700"><AlertCircle size={17} className="mt-0.5 shrink-0"/>{error}</div>}<div className="border-t border-slate-100 pt-5"><button disabled={busy} className="btn-primary w-full py-3">{busy?"Submitting application…":"Submit application"}<ArrowRight size={17}/></button><p className="mt-4 text-center text-sm text-slate-500">Already have an account? <Link className="font-semibold text-blue-600" href="/login">Sign in</Link></p></div></form></div></section></main>;
}
