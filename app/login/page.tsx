"use client"; import {useState} from "react"; import {ShieldCheck,LockKeyhole,Eye,EyeOff,Fingerprint,ArrowRight,AlertCircle} from "lucide-react"; import Link from "next/link"; import {useRouter} from "next/navigation"; import {authApi} from "@/services/api";
export default function LoginPage(){const [show,setShow]=useState(false);const [username,setUsername]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError("");try{const r=await authApi.login({username,password});const token=(r as any).access??(r as any).accessToken;if(!token) throw new Error("The API did not return an access token."); localStorage.setItem("sddms_access_token",token); try { const me=await authApi.me(); localStorage.setItem("sddms_user",JSON.stringify((me as any)?.data ?? me)); } catch {} router.push("/dashboard")}catch(e:any){setError(e?.message??"Sign in failed. Check the API server and credentials.")}finally{setBusy(false)}};return <main className="min-h-screen bg-[#f5f7fb] lg:grid lg:grid-cols-2"><section className="relative hidden overflow-hidden bg-navy p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-24 -top-24 h-72 w-72 rounded-full bg-blue-500/20 blur-3xl"/><div className="flex items-center gap-3"><div className="rounded-xl bg-white/10 p-2.5"><ShieldCheck size={26}/></div><div><p className="font-bold">Secure DMS</p><p className="text-xs text-blue-100">Legal & Investigation Records</p></div></div><div className="relative max-w-xl"><p className="mb-4 text-sm font-semibold uppercase tracking-[.2em] text-blue-200">Trusted document control</p><h1 className="text-5xl font-bold leading-tight">Protect every case file from intake to archive.</h1><p className="mt-6 max-w-lg text-lg leading-8 text-blue-100">Centralized legal, police, forensic and investigation records with role-based access, integrity verification, audit trails and assisted intelligence.</p><div className="mt-8 grid grid-cols-3 gap-3">{["Access control","Integrity","AI-assisted"].map(x=><div key={x} className="glass rounded-xl p-4 text-sm">{x}</div>)}</div></div><p className="text-xs text-blue-200">© 2026 Secure DMS • Authorized personnel only</p></section><section className="flex min-h-screen items-center justify-center p-5 sm:p-10"><div className="w-full max-w-md"><div className="mb-7 lg:hidden"><div className="inline-flex items-center gap-3"><div className="rounded-xl bg-navy p-2.5 text-white"><ShieldCheck size={24}/></div><span className="font-bold text-navy">Secure DMS</span></div></div><div className="card p-6 sm:p-9"><div className="mb-7"><p className="text-sm font-semibold text-blue-600">Secure sign in</p><h2 className="mt-1 text-3xl font-bold text-slate-900">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Sign in to access protected case records.</p></div><form onSubmit={submit} className="space-y-5"><div><label className="label">Email / Username</label><input className="input" value={username} onChange={e=>setUsername(e.target.value)} placeholder="officer@agency.gov" required/></div><div><label className="label">Password</label><div className="relative"><input className="input pr-11" value={password} onChange={e=>setPassword(e.target.value)} type={show?"text":"password"} placeholder="Enter your password" required/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></div><div className="flex items-center justify-between text-sm"><label className="flex items-center gap-2 text-slate-600"><input type="checkbox" className="h-4 w-4 rounded"/>Remember me</label><Link href="#" className="font-semibold text-blue-600">Forgot password?</Link></div>{error&&<div className="flex gap-2 rounded-xl border border-red-100 bg-red-50 p-3 text-xs leading-5 text-red-700"><AlertCircle className="mt-0.5 shrink-0" size={16}/>{error}</div>}<button disabled={busy} className="btn-primary w-full py-3">{busy?"Signing in…":<> <LockKeyhole size={17}/>Sign in<ArrowRight size={17}/></>}</button></form><div className="my-6 flex items-center gap-3"><div className="h-px flex-1 bg-slate-200"/><span className="text-xs text-slate-400">OR</span><div className="h-px flex-1 bg-slate-200"/></div><button className="btn-secondary w-full"><Fingerprint size={18}/>Use secure identity / SSO</button></div><p className="mt-5 text-center text-sm text-slate-500">Need access? <Link href="/register" className="font-semibold text-blue-600">Apply for an account</Link></p><p className="mt-2 text-center text-xs text-slate-400">Protected environment • Unauthorized access is prohibited</p></div></section></main>}
