"use client";
import {useEffect,useState} from "react"; import {usePathname,useRouter} from "next/navigation"; import {authApi} from "@/services/api"; import {Loading} from "./Loading";
export default function RoleGuard({children}:{children:React.ReactNode}){const router=useRouter();const path=usePathname();const [ready,setReady]=useState(false);useEffect(()=>{let mounted=true;const run=async()=>{try{const r:any=await authApi.me();const u=r?.data??r;if(mounted){localStorage.setItem("sddms_user",JSON.stringify(u));if(u?.role==="NORMAL_USER"&&!(["/dashboard","/dashboard/complaints","/dashboard/complaints/new"].some(p=>path===p||path.startsWith(`${p}/`)))){router.replace("/dashboard");return}}}catch{if(mounted)router.replace("/login")}finally{if(mounted)setReady(true)}};run();return()=>{mounted=false}},[path,router]);return ready?<>{children}</>:<Loading/>}
